[tool.poetry]
name = "syakyo"
version = "0.1.0"
description = "Test"
authors = ["kuchidareo"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^2.2.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
